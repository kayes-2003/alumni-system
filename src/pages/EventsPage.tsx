import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Events"
      description="Discover upcoming reunions, webinars, and networking events. Register, get your QR code, and check in on arrival."
    />
  );
}
